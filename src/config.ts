export const config = {
  // Set to true to publish the CV page in production.
  // When false, the CV page will be hidden/redirected in production builds but fully accessible in development.
  publishCV: false,
};
